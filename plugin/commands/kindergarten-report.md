---
description: Generate an adult-readable progress report from confirmed local evidence.
argumentHint: "[student-id] [time-window]"
model: inherit
---

Delegate to `generate-progress-report` with `$ARGUMENTS`. Use a read-only snapshot, distinguish confirmed and unconfirmed evidence, and store a new report artifact without overwriting history.
