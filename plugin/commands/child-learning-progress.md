---
description: Apply confirmed evidence to concept-level progress through policy.
argumentHint: "[student-id] [evaluation-id]"
model: inherit
---

Delegate to `track-progress` and, when needed, `adjust-difficulty` with `$ARGUMENTS`. Let application code calculate and write append-only events. Claude must not directly mutate progress and must report only confirmed writes.
