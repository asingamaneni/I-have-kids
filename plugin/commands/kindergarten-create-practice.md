---
description: Create a validated practice activity for a subject and concept.
argumentHint: "[subject] [concept] [difficulty] [question-count]"
model: inherit
---

Delegate to the `create-practice` skill with `$ARGUMENTS`. Load local state, create the activity specification before rendering, and use deterministic application validation. Use only actual local MCP results and report unavailable steps.
